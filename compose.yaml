services:
  redis:
      image: redis:latest
      container_name: redis
      restart: always
      ports:
        - "6379:6379"

  application_service:
    build:
      context: ./src/application_service
      dockerfile: Dockerfile.application_service
    container_name: application_service
    ports:
      - "8000:80"
    volumes:
      - .:/code
    command: uvicorn src.application_service.entrypoints.main:app --host 0.0.0.0 --port 80 --reload
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379

  notification_services:
    build:
      context: ./src/notification_services
      dockerfile: Dockerfile.notification_services
    container_name: notification_services
    volumes:
      - .:/code
    command: python src/notification_services/worker.py
    depends_on:
          - redis
    environment:
      - REDIS_HOST=redis
      - REDIS_PORT=6379
