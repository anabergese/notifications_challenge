services:
  customer_requests:
    build: .
    ports:
      - "8000:80"
    develop:
      watch:
        - action: sync
          path: .
          target: /code

  notifications:
    build: .
    ports:
      - "8001:80"